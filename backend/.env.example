# ─── ERP Marcenaria v2.0 — Variáveis de Ambiente ──────────────────
# Copie para .env e ajuste os valores

PORT=3001
NODE_ENV=development

# JWT — MUDE ESTA CHAVE EM PRODUÇÃO!
JWT_SECRET=erp_marcenaria_chave_super_secreta_mude_em_producao_2026
JWT_EXPIRES=8h

# Origens permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001
